export interface ServiceData {
  id: string;
  title: string;
  description: string;
  path: string;
  iconName: string;
}

export interface ProgramData {
  id: string;
  title: string;
  subtitle: string;
  price: number;
  description: string;
  path: string;
  features: string[];
}
